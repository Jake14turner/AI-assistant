This is a python application that utalizes openai's assistant api. Using streamlit as a front end. This assistant can accept any uploaded files and read off of them into its knoledge base. Creates a thread and sticks with it to ensure that the conversation continues. to run use the command: streamlit run main.py. Additionally you will have to use your own api key on line 17.
